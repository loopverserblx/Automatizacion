<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Planes y Precios | AutoFlow TikTok</title>
    
    <meta name="description" content="Elige tu motor de crecimiento. Planes de automatización TikTok con n8n, IA y soporte oficial API.">
    
    <style>
        :root {
            --primary: #fe2c55; /* TikTok Red */
            --secondary: #25f4ee; /* TikTok Cyan */
            --bg-dark: #0a0a0b;
            --card-bg: rgba(255, 255, 255, 0.03);
            --text-main: #f1f1f1;
            --glass: rgba(255, 255, 255, 0.05);
            --glow: 0 0 25px rgba(254, 44, 85, 0.3);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            background-color: var(--bg-dark); 
            color: var(--text-main); 
            font-family: 'Inter', system-ui, sans-serif; 
            line-height: 1.5;
            overflow-x: hidden;
        }

        /* HEADER (REPLICADO EXACTO) */
        header {
            position: fixed;
            top: 0; width: 100%;
            padding: 20px 5%;
            display: flex; justify-content: space-between; align-items: center;
            background: rgba(10, 10, 11, 0.8);
            backdrop-filter: blur(20px);
            z-index: 1000;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        .logo { font-weight: 900; font-size: 1.6rem; letter-spacing: -1.5px; }
        .logo span { color: var(--primary); text-shadow: var(--glow); }
        nav a { color: #aaa; text-decoration: none; margin: 0 15px; font-size: 0.9rem; transition: 0.3s; font-weight: 500; }
        nav a:hover { color: var(--secondary); }

        /* HERO PRECIOS */
        .pricing-hero {
            padding: 180px 5% 60px;
            text-align: center;
            background: radial-gradient(circle at 50% 0%, rgba(37, 244, 238, 0.1) 0%, transparent 50%);
        }
        .pricing-hero h1 {
            font-size: clamp(3rem, 8vw, 5.5rem);
            font-weight: 900; letter-spacing: -4px; line-height: 0.9;
            margin-bottom: 20px;
        }

        /* TOGGLE ANUAL/MENSUAL */
        .pricing-toggle {
            display: flex; align-items: center; justify-content: center;
            gap: 20px; margin-bottom: 60px;
        }
        .toggle-label { font-size: 0.9rem; font-weight: 700; color: #666; transition: 0.3s; }
        .toggle-label.active { color: #fff; }
        .switch {
            position: relative; display: inline-block; width: 60px; height: 34px;
        }
        .switch input { opacity: 0; width: 0; height: 0; }
        .slider {
            position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0;
            background-color: var(--glass); transition: .4s; border-radius: 34px;
            border: 1px solid rgba(255,255,255,0.1);
        }
        .slider:before {
            position: absolute; content: ""; height: 26px; width: 26px; left: 4px; bottom: 3px;
            background-color: var(--primary); transition: .4s; border-radius: 50%;
            box-shadow: var(--glow);
        }
        input:checked + .slider:before { transform: translateX(26px); background-color: var(--secondary); }

        /* GRID DE PLANES */
        .pricing-grid {
            max-width: 1200px; margin: 0 auto 100px;
            display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px; padding: 0 5%;
        }
        .pricing-card {
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 40px; padding: 50px 40px;
            position: relative; transition: 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex; flex-direction: column;
        }
        .pricing-card:hover {
            transform: translateY(-15px) scale(1.02);
            border-color: rgba(255,255,255,0.2);
            background: rgba(255, 255, 255, 0.04);
        }
        .pricing-card.featured {
            border: 2px solid var(--primary);
            background: rgba(254, 44, 85, 0.03);
            box-shadow: 0 20px 40px rgba(254, 44, 85, 0.1);
        }
        .featured-badge {
            position: absolute; top: -15px; left: 50%; transform: translateX(-50%);
            background: var(--primary); color: #fff; padding: 5px 20px;
            border-radius: 50px; font-size: 0.7rem; font-weight: 900; letter-spacing: 1px;
        }

        .plan-name { font-size: 1.2rem; font-weight: 800; color: #888; margin-bottom: 10px; }
        .plan-price { font-size: 4rem; font-weight: 900; margin-bottom: 30px; letter-spacing: -2px; }
        .plan-price span { font-size: 1rem; color: #555; letter-spacing: 0; }
        
        .plan-features { list-style: none; margin-bottom: 40px; flex-grow: 1; }
        .plan-features li { margin-bottom: 15px; color: #aaa; font-size: 0.95rem; display: flex; align-items: center; gap: 10px; }
        .plan-features li::before { content: "✓"; color: var(--secondary); font-weight: 900; }

        .cta-pricing {
            width: 100%; text-align: center; padding: 20px;
            background: var(--glass); border: 1px solid rgba(255,255,255,0.1);
            color: #fff; border-radius: 20px; text-decoration: none;
            font-weight: 800; transition: 0.3s;
        }
        .pricing-card.featured .cta-pricing {
            background: var(--primary); box-shadow: var(--glow); border: none;
        }
        .cta-pricing:hover { transform: scale(1.05); background: #fff; color: #000; }

        /* FAQ RAPIDA */
        .faq-lite { max-width: 800px; margin: 0 auto 100px; padding: 0 5%; }
        .faq-item { margin-bottom: 30px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 20px; }
        .faq-item h4 { margin-bottom: 10px; color: var(--secondary); }
        .faq-item p { color: #666; font-size: 0.9rem; }

        /* FOOTER (REPLICADO EXACTO) */
        footer {
            padding: 100px 5% 50px; background: #050506;
            border-top: 1px solid rgba(255,255,255,0.05);
            display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 50px;
        }
        .footer-logo { font-size: 1.5rem; font-weight: 900; margin-bottom: 20px; }
        footer h5 { color: var(--secondary); margin-bottom: 20px; font-size: 1rem; }
        footer ul { list-style: none; }
        footer ul li { margin-bottom: 10px; }
        footer a { color: #666; text-decoration: none; font-size: 0.9rem; transition: 0.3s; }
        footer a:hover { color: #fff; }

        @media (max-width: 768px) {
            h1 { font-size: 3.5rem; }
            .pricing-grid { grid-template-columns: 1fr; }
            footer { grid-template-columns: 1fr; text-align: center; }
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">AUTO<span>FLOW</span></div>
        <nav>
            <a href="index.html">Inicio</a>
            <a href="sistemas.html">Sistemas</a>
            <a href="casos-exito.html">Casos Éxito</a>
            <a href="blog.html">Blog</a>
        </nav>
        <a href="precios.html" style="color: var(--secondary); border: 1px solid var(--secondary); padding: 10px 25px; border-radius: 12px; text-decoration: none; font-size: 0.8rem; font-weight: 800;">Planes</a>
    </header>

    <section class="pricing-hero">
        <h1>Invierte en tu<br>Libertad Digital.</h1>
        <p style="color: #666; margin-bottom: 40px;">Tu tiempo vale más que el precio de nuestros flujos de trabajo.</p>
        
        <div class="pricing-toggle">
            <span class="toggle-label active" id="label-monthly">Mensual</span>
            <label class="switch">
                <input type="checkbox" id="billing-switch">
                <span class="slider"></span>
            </label>
            <span class="toggle-label" id="label-yearly">Anual <small style="color: var(--secondary);">(Ahorra 20%)</small></span>
        </div>
    </section>

    <div class="pricing-grid">
        
        <!-- PLAN START -->
        <div class="pricing-card">
            <div class="plan-name">Starter</div>
            <div class="plan-price"><span class="currency">€</span><span class="amount" data-monthly="49" data-yearly="39">49</span><span class="period">/mes</span></div>
            <ul class="plan-features">
                <li>1 Vídeo diario automático</li>
                <li>Conexión n8n Cloud / Local</li>
                <li>Voz Standard AI</li>
                <li>API Oficial TikTok v3</li>
                <li>Soporte por Ticket</li>
            </ul>
            <a href="#" class="cta-pricing">Empezar Ahora</a>
        </div>

        <!-- PLAN GROWTH (FEATURED) -->
        <div class="pricing-card featured">
            <div class="featured-badge">MÁS POPULAR</div>
            <div class="plan-name" style="color: #fff;">Growth Pro</div>
            <div class="plan-price" style="color: var(--secondary);"><span class="currency">€</span><span class="amount" data-monthly="99" data-yearly="79">99</span><span class="period">/mes</span></div>
            <ul class="plan-features">
                <li>3 Vídeos diarios (Multicuentas)</li>
                <li>Voz ElevenLabs Hi-Res</li>
                <li>Análisis de Tendencias GPT-4o</li>
                <li>SEO Dinámico OCR/NLP</li>
                <li>Soporte Prioritario 24/7</li>
                <li>Sin marca de agua IA</li>
            </ul>
            <a href="#" class="cta-pricing">Dominar TikTok</a>
        </div>

        <!-- PLAN AGENCY -->
        <div class="pricing-card">
            <div class="plan-name">Elite / Agency</div>
            <div class="plan-price"><span class="currency">€</span><span class="amount" data-monthly="249" data-yearly="199">249</span><span class="period">/mes</span></div>
            <ul class="plan-features">
                <li>Vídeos Ilimitados</li>
                <li>Gestión de hasta 10 canales</li>
                <li>Entrenamiento de Modelo Propio</li>
                <li>Integración WooCommerce Directa</li>
                <li>Manager de Cuenta Dedicado</li>
                <li>Dashboard de Métricas n8n</li>
            </ul>
            <a href="#" class="cta-pricing">Contactar Ventas</a>
        </div>

    </div>

    <section class="faq-lite">
        <h3 style="text-align: center; margin-bottom: 50px; font-size: 2rem;">Preguntas Frecuentes</h3>
        <div class="faq-item">
            <h4>¿Puedo cancelar en cualquier momento?</h4>
            <p>Sí, no hay permanencia. Si cancelas, el sistema seguirá funcionando hasta que termine tu periodo de facturación actual.</p>
        </div>
        <div class="faq-item">
            <h4>¿Necesito conocimientos de n8n?</h4>
            <p>No. Nosotros configuramos el sistema por ti. Tú solo introduces el título en el panel y AutoFlow se encarga del resto.</p>
        </div>
        <div class="faq-item">
            <h4>¿Es seguro para mi cuenta de TikTok?</h4>
            <p>Utilizamos la API oficial v3 de TikTok for Developers, cumpliendo al 100% sus términos de servicio para evitar shadowbans.</p>
        </div>
    </section>

    <footer>
        <div>
            <div class="footer-logo">AUTO<span>FLOW</span></div>
            <p style="color: #555; font-size: 0.8rem;">Líderes en automatización de sistemas de atención y medios. Construyendo el futuro de la economía de creadores.</p>
        </div>
        <div>
            <h5>Soluciones</h5>
            <ul>
                <li><a href="#">Automatización B2B</a></li>
                <li><a href="#">Canales Faceless</a></li>
                <li><a href="#">API Direct Post</a></li>
            </ul>
        </div>
        <div>
            <h5>Recursos</h5>
            <ul>
                <li><a href="#">Documentación n8n</a></li>
                <li><a href="#">Blog de IA</a></li>
                <li><a href="#">Casos de Éxito</a></li>
            </ul>
        </div>
        <div>
            <h5>Legal</h5>
            <ul>
                <li><a href="#">Términos de Servicio</a></li>
                <li><a href="#">Protección de Datos</a></li>
                <li><a href="#">Política de Cookies</a></li>
            </ul>
        </div>
    </footer>

    <script>
        const billingSwitch = document.getElementById('billing-switch');
        const amounts = document.querySelectorAll('.amount');
        const labelMonthly = document.getElementById('label-monthly');
        const labelYearly = document.getElementById('label-yearly');

        billingSwitch.addEventListener('change', () => {
            const isYearly = billingSwitch.checked;
            
            amounts.forEach(amount => {
                const targetPrice = isYearly ? amount.getAttribute('data-yearly') : amount.getAttribute('data-monthly');
                
                // Animación de cambio de número
                amount.style.opacity = '0';
                amount.style.transform = 'translateY(10px)';
                
                setTimeout(() => {
                    amount.innerText = targetPrice;
                    amount.style.opacity = '1';
                    amount.style.transform = 'translateY(0)';
                }, 200);
            });

            if(isYearly) {
                labelYearly.classList.add('active');
                labelMonthly.classList.remove('active');
            } else {
                labelMonthly.classList.add('active');
                labelYearly.classList.remove('active');
            }
        });

        // Revelado suave
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = "1";
                    entry.target.style.transform = "translateY(0)";
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.pricing-card, .faq-item').forEach(el => {
            el.style.opacity = "0";
            el.style.transform = "translateY(30px)";
            el.style.transition = "0.8s ease-out";
            observer.observe(el);
        });
    </script>
</body>
</html>